pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo 'Checking out code'
            }
        }

        stage('Build') {
            steps {
                echo 'Building docker image'
            }
        }

        stage('Run') {
            steps {
                echo 'Running docker image'
            }
        }
    }
}